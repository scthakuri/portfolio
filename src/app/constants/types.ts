export type ProjectType = {
    id: number,
    title: string,
    date: string,
    description: string,
    image: string,
    tags: string[],
    category: string,
    github: string,
    webapp: string,
}